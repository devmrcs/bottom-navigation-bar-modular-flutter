import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Tab1Page has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(Tab1Page(title: 'Tab1')));
    //  final titleFinder = find.text('Tab1');
    //  expect(titleFinder, findsOneWidget);
  });
}
